# Legacy prototype

`index.html` preserves the standalone file formerly stored as `AI Vocal Architect/Legacy Prototype/index.html.html`. Its bytes are unchanged; only the redundant file extension was removed.

This is historical reference, not the current application entry point. Work on the React application in `src/`. See `docs/rebuild-comparison.md` for the relationship between versions.

SHA-256: `42F9BECA165D26843193BCDCFB473EF23D4C219F74A4AF6443DC893339B63C54`.
