# JSON Examples

This folder contains example structured chain exports produced by the system.

These files are useful for:

- documenting chain output format
- validating downstream integrations
- demonstrating how presets translate into structured processing data

The included example is representative of the current export schema used by the app.
